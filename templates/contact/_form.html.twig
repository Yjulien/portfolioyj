
{% block title %}Contactez moi ! - Yjulien {% endblock %}
{% extends 'base.html.twig' %}

{% block body %}

        <!-- Contact Section-->
        <section class="page-section" id="contact">
            <div class="container">
                <!-- Contact Section Heading-->
                <h2 class="page-section-heading text-center text-uppercase text-secondary mb-0">Contactez moi</h2>
                <div class="divider-custom text-center">
                <p class="mb-4">Si vous avez des questions ou des commentaires, n'hésitez pas à me contacter en remplissant le formulaire ci-dessous. Je me ferai un plaisir de vous répondre dans les plus brefs délais.</p>
                </div>
                <!-- Icon Divider-->
                <div class="divider-custom">
                    <div class="divider-custom-line"></div>
                    <div class="divider-custom-icon"><i class="fas fa-star"></i></div>
                    <div class="divider-custom-line"></div>
                </div>
                <!-- Contact Section Form-->
                <div class="row justify-content-center">
                
                    <div class="col-lg-8 col-xl-7">
                    {{ form(formulaire) }}
                    </div>
                    
                </div>
                <a href="{{ path('app_home') }}" class="btn btn-primary btn-bottom-right">Revenir à la page d'accueil</a>
               
                {% if app.session.flashBag.has('success') %}
                    <div class="alert alert-success" data-delay="5000">
                        {% for message in app.session.flashBag.get('success') %}
                            {{ message }}
                        {% endfor %}
                    </div>
                {% endif %}
           
            </div>
            



{% endblock %}
